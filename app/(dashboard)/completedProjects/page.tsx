import CompletedProjectsList from "@/components/main/completedProjects";

export default function CompletedProjects() {
    return (
        <div>
            <CompletedProjectsList />
        </div>
    );
}